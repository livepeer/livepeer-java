/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package studio.livepeer.livepeer;

import static studio.livepeer.livepeer.operations.Operations.AsyncRequestlessOperation;
import static studio.livepeer.livepeer.operations.Operations.AsyncRequestOperation;

import java.lang.String;
import java.util.concurrent.CompletableFuture;
import studio.livepeer.livepeer.models.components.AssetPatchPayload;
import studio.livepeer.livepeer.models.components.NewAssetFromUrlPayload;
import studio.livepeer.livepeer.models.components.NewAssetPayload;
import studio.livepeer.livepeer.models.operations.DeleteAssetRequest;
import studio.livepeer.livepeer.models.operations.GetAssetRequest;
import studio.livepeer.livepeer.models.operations.UpdateAssetRequest;
import studio.livepeer.livepeer.models.operations.async.DeleteAssetRequestBuilder;
import studio.livepeer.livepeer.models.operations.async.DeleteAssetResponse;
import studio.livepeer.livepeer.models.operations.async.GetAssetRequestBuilder;
import studio.livepeer.livepeer.models.operations.async.GetAssetResponse;
import studio.livepeer.livepeer.models.operations.async.GetAssetsRequestBuilder;
import studio.livepeer.livepeer.models.operations.async.GetAssetsResponse;
import studio.livepeer.livepeer.models.operations.async.RequestUploadRequestBuilder;
import studio.livepeer.livepeer.models.operations.async.RequestUploadResponse;
import studio.livepeer.livepeer.models.operations.async.UpdateAssetRequestBuilder;
import studio.livepeer.livepeer.models.operations.async.UpdateAssetResponse;
import studio.livepeer.livepeer.models.operations.async.UploadAssetRequestBuilder;
import studio.livepeer.livepeer.models.operations.async.UploadAssetResponse;
import studio.livepeer.livepeer.operations.DeleteAsset;
import studio.livepeer.livepeer.operations.GetAsset;
import studio.livepeer.livepeer.operations.GetAssets;
import studio.livepeer.livepeer.operations.RequestUpload;
import studio.livepeer.livepeer.operations.UpdateAsset;
import studio.livepeer.livepeer.operations.UploadAsset;
import studio.livepeer.livepeer.utils.Headers;

/**
 * Operations related to asset/vod api
 */
public class AsyncAsset {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final Asset syncSDK;

    AsyncAsset(Asset syncSDK, SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.syncSDK = syncSDK;
    }

    /**
     * Switches to the sync SDK.
     * 
     * @return The sync SDK
     */
    public Asset sync() {
        return syncSDK;
    }


    /**
     * Retrieve assets
     * 
     * @return The async call builder
     */
    public GetAssetsRequestBuilder getAll() {
        return new GetAssetsRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieve assets
     * 
     * @return {@code CompletableFuture<GetAssetsResponse>} - The async response
     */
    public CompletableFuture<GetAssetsResponse> getAllDirect() {
        AsyncRequestlessOperation<GetAssetsResponse> operation
            = new GetAssets.Async(sdkConfiguration, _headers);
        return operation.doRequest()
            .thenCompose(operation::handleResponse);
    }


    /**
     * Upload an asset
     * 
     * <p>To upload an asset, your first need to request for a direct upload URL
     * and only then actually upload the contents of the asset.
     * \
     * \
     * Once you created a upload link, you have 2 options, resumable or direct
     * upload. For a more reliable experience, you should use resumable uploads
     * which will work better for users with unreliable or slow network
     * connections. If you want a simpler implementation though, you should
     * just use a direct upload.
     * 
     * <p>## Direct Upload
     * For a direct upload, make a PUT request to the URL received in the url
     * field of the response above, with the raw video file as the request
     * body. response above:
     * 
     * <p>## Resumable Upload
     * Livepeer supports resumable uploads via Tus. This section provides a
     * simple example of how to use tus-js-client to upload a video file.
     * \
     * \
     * From the previous section, we generated a URL to upload a video file to
     * Livepeer on POST /api/asset/request-upload. You should use the
     * tusEndpoint field of the response to upload the video file and track the
     * progress:
     * 
     * <p>```
     * # This assumes there is an `input` element of `type="file"` with id
     * `fileInput` in the HTML
     * 
     * <p>const input = document.getElementById('fileInput');
     * 
     * <p>const file = input.files[0];
     * 
     * <p>const upload = new tus.Upload(file, {
     * endpoint: tusEndpoint, // URL from `tusEndpoint` field in the
     * `/request-upload` response
     * metadata: {
     * filename,
     * filetype: 'video/mp4',
     * },
     * uploadSize: file.size,
     * onError(err) {
     * console.error('Error uploading file:', err);
     * },
     * onProgress(bytesUploaded, bytesTotal) {
     * const percentage = ((bytesUploaded / bytesTotal) * 100).toFixed(2);
     * console.log('Uploaded ' + percentage + '%');
     * },
     * onSuccess() {
     * console.log('Upload finished:', upload.url);
     * },
     * });
     * 
     * <p>const previousUploads = await upload.findPreviousUploads();
     * 
     * <p>if (previousUploads.length &gt; 0) {
     * upload.resumeFromPreviousUpload(previousUploads[0]);
     * }
     * 
     * <p>upload.start();
     * 
     * <p>```
     * 
     * <p>&gt; Note: If you are using tus from node.js, you need to add a custom URL
     * storage to enable resuming from previous uploads. On the browser, this
     * is enabled by default using local storage. In node.js, add urlStorage:
     * new tus.FileUrlStorage("path/to/tmp/file"), to the UploadFile object
     * definition above.
     * 
     * @return The async call builder
     */
    public RequestUploadRequestBuilder create() {
        return new RequestUploadRequestBuilder(sdkConfiguration);
    }

    /**
     * Upload an asset
     * 
     * <p>To upload an asset, your first need to request for a direct upload URL
     * and only then actually upload the contents of the asset.
     * \
     * \
     * Once you created a upload link, you have 2 options, resumable or direct
     * upload. For a more reliable experience, you should use resumable uploads
     * which will work better for users with unreliable or slow network
     * connections. If you want a simpler implementation though, you should
     * just use a direct upload.
     * 
     * <p>## Direct Upload
     * For a direct upload, make a PUT request to the URL received in the url
     * field of the response above, with the raw video file as the request
     * body. response above:
     * 
     * <p>## Resumable Upload
     * Livepeer supports resumable uploads via Tus. This section provides a
     * simple example of how to use tus-js-client to upload a video file.
     * \
     * \
     * From the previous section, we generated a URL to upload a video file to
     * Livepeer on POST /api/asset/request-upload. You should use the
     * tusEndpoint field of the response to upload the video file and track the
     * progress:
     * 
     * <p>```
     * # This assumes there is an `input` element of `type="file"` with id
     * `fileInput` in the HTML
     * 
     * <p>const input = document.getElementById('fileInput');
     * 
     * <p>const file = input.files[0];
     * 
     * <p>const upload = new tus.Upload(file, {
     * endpoint: tusEndpoint, // URL from `tusEndpoint` field in the
     * `/request-upload` response
     * metadata: {
     * filename,
     * filetype: 'video/mp4',
     * },
     * uploadSize: file.size,
     * onError(err) {
     * console.error('Error uploading file:', err);
     * },
     * onProgress(bytesUploaded, bytesTotal) {
     * const percentage = ((bytesUploaded / bytesTotal) * 100).toFixed(2);
     * console.log('Uploaded ' + percentage + '%');
     * },
     * onSuccess() {
     * console.log('Upload finished:', upload.url);
     * },
     * });
     * 
     * <p>const previousUploads = await upload.findPreviousUploads();
     * 
     * <p>if (previousUploads.length &gt; 0) {
     * upload.resumeFromPreviousUpload(previousUploads[0]);
     * }
     * 
     * <p>upload.start();
     * 
     * <p>```
     * 
     * <p>&gt; Note: If you are using tus from node.js, you need to add a custom URL
     * storage to enable resuming from previous uploads. On the browser, this
     * is enabled by default using local storage. In node.js, add urlStorage:
     * new tus.FileUrlStorage("path/to/tmp/file"), to the UploadFile object
     * definition above.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return {@code CompletableFuture<RequestUploadResponse>} - The async response
     */
    public CompletableFuture<RequestUploadResponse> create(NewAssetPayload request) {
        AsyncRequestOperation<NewAssetPayload, RequestUploadResponse> operation
              = new RequestUpload.Async(sdkConfiguration, _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Upload asset via URL
     * 
     * @return The async call builder
     */
    public UploadAssetRequestBuilder createViaUrl() {
        return new UploadAssetRequestBuilder(sdkConfiguration);
    }

    /**
     * Upload asset via URL
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return {@code CompletableFuture<UploadAssetResponse>} - The async response
     */
    public CompletableFuture<UploadAssetResponse> createViaUrl(NewAssetFromUrlPayload request) {
        AsyncRequestOperation<NewAssetFromUrlPayload, UploadAssetResponse> operation
              = new UploadAsset.Async(sdkConfiguration, _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Retrieves an asset
     * 
     * @return The async call builder
     */
    public GetAssetRequestBuilder get() {
        return new GetAssetRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieves an asset
     * 
     * @param assetId ID of the asset
     * @return {@code CompletableFuture<GetAssetResponse>} - The async response
     */
    public CompletableFuture<GetAssetResponse> get(String assetId) {
        GetAssetRequest request =
            GetAssetRequest
                .builder()
                .assetId(assetId)
                .build();
        AsyncRequestOperation<GetAssetRequest, GetAssetResponse> operation
              = new GetAsset.Async(sdkConfiguration, _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Patch an asset
     * 
     * @return The async call builder
     */
    public UpdateAssetRequestBuilder update() {
        return new UpdateAssetRequestBuilder(sdkConfiguration);
    }

    /**
     * Patch an asset
     * 
     * @param assetId ID of the asset
     * @param assetPatchPayload 
     * @return {@code CompletableFuture<UpdateAssetResponse>} - The async response
     */
    public CompletableFuture<UpdateAssetResponse> update(String assetId, AssetPatchPayload assetPatchPayload) {
        UpdateAssetRequest request =
            UpdateAssetRequest
                .builder()
                .assetId(assetId)
                .assetPatchPayload(assetPatchPayload)
                .build();
        AsyncRequestOperation<UpdateAssetRequest, UpdateAssetResponse> operation
              = new UpdateAsset.Async(sdkConfiguration, _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Delete an asset
     * 
     * @return The async call builder
     */
    public DeleteAssetRequestBuilder delete() {
        return new DeleteAssetRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete an asset
     * 
     * @param assetId ID of the asset
     * @return {@code CompletableFuture<DeleteAssetResponse>} - The async response
     */
    public CompletableFuture<DeleteAssetResponse> delete(String assetId) {
        DeleteAssetRequest request =
            DeleteAssetRequest
                .builder()
                .assetId(assetId)
                .build();
        AsyncRequestOperation<DeleteAssetRequest, DeleteAssetResponse> operation
              = new DeleteAsset.Async(sdkConfiguration, _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

}
