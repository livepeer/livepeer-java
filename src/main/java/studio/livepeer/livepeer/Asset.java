/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package studio.livepeer.livepeer;

import static studio.livepeer.livepeer.operations.Operations.RequestlessOperation;
import static studio.livepeer.livepeer.operations.Operations.RequestOperation;

import java.lang.Exception;
import java.lang.String;
import studio.livepeer.livepeer.models.components.AssetPatchPayload;
import studio.livepeer.livepeer.models.components.NewAssetFromUrlPayload;
import studio.livepeer.livepeer.models.components.NewAssetPayload;
import studio.livepeer.livepeer.models.operations.DeleteAssetRequest;
import studio.livepeer.livepeer.models.operations.DeleteAssetRequestBuilder;
import studio.livepeer.livepeer.models.operations.DeleteAssetResponse;
import studio.livepeer.livepeer.models.operations.GetAssetRequest;
import studio.livepeer.livepeer.models.operations.GetAssetRequestBuilder;
import studio.livepeer.livepeer.models.operations.GetAssetResponse;
import studio.livepeer.livepeer.models.operations.GetAssetsRequestBuilder;
import studio.livepeer.livepeer.models.operations.GetAssetsResponse;
import studio.livepeer.livepeer.models.operations.RequestUploadRequestBuilder;
import studio.livepeer.livepeer.models.operations.RequestUploadResponse;
import studio.livepeer.livepeer.models.operations.UpdateAssetRequest;
import studio.livepeer.livepeer.models.operations.UpdateAssetRequestBuilder;
import studio.livepeer.livepeer.models.operations.UpdateAssetResponse;
import studio.livepeer.livepeer.models.operations.UploadAssetRequestBuilder;
import studio.livepeer.livepeer.models.operations.UploadAssetResponse;
import studio.livepeer.livepeer.operations.DeleteAsset;
import studio.livepeer.livepeer.operations.GetAsset;
import studio.livepeer.livepeer.operations.GetAssets;
import studio.livepeer.livepeer.operations.RequestUpload;
import studio.livepeer.livepeer.operations.UpdateAsset;
import studio.livepeer.livepeer.operations.UploadAsset;
import studio.livepeer.livepeer.utils.Headers;

/**
 * Operations related to asset/vod api
 */
public class Asset {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final AsyncAsset asyncSDK;

    Asset(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.asyncSDK = new AsyncAsset(this, sdkConfiguration);
    }

    /**
     * Switches to the async SDK.
     * 
     * @return The async SDK
     */
    public AsyncAsset async() {
        return asyncSDK;
    }

    /**
     * Retrieve assets
     * 
     * @return The call builder
     */
    public GetAssetsRequestBuilder getAll() {
        return new GetAssetsRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieve assets
     * 
     * @return The response from the API call
     * @throws Exception if the API call fails
     */
    public GetAssetsResponse getAllDirect() throws Exception {
        RequestlessOperation<GetAssetsResponse> operation
            = new GetAssets.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest());
    }

    /**
     * Upload an asset
     * 
     * <p>To upload an asset, your first need to request for a direct upload URL
     * and only then actually upload the contents of the asset.
     * \
     * \
     * Once you created a upload link, you have 2 options, resumable or direct
     * upload. For a more reliable experience, you should use resumable uploads
     * which will work better for users with unreliable or slow network
     * connections. If you want a simpler implementation though, you should
     * just use a direct upload.
     * 
     * <p>## Direct Upload
     * For a direct upload, make a PUT request to the URL received in the url
     * field of the response above, with the raw video file as the request
     * body. response above:
     * 
     * <p>## Resumable Upload
     * Livepeer supports resumable uploads via Tus. This section provides a
     * simple example of how to use tus-js-client to upload a video file.
     * \
     * \
     * From the previous section, we generated a URL to upload a video file to
     * Livepeer on POST /api/asset/request-upload. You should use the
     * tusEndpoint field of the response to upload the video file and track the
     * progress:
     * 
     * <p>```
     * # This assumes there is an `input` element of `type="file"` with id
     * `fileInput` in the HTML
     * 
     * <p>const input = document.getElementById('fileInput');
     * 
     * <p>const file = input.files[0];
     * 
     * <p>const upload = new tus.Upload(file, {
     *   endpoint: tusEndpoint, // URL from `tusEndpoint` field in the
     * `/request-upload` response
     *   metadata: {
     *     filename,
     *     filetype: 'video/mp4',
     *   },
     *   uploadSize: file.size,
     *   onError(err) {
     *     console.error('Error uploading file:', err);
     *   },
     *   onProgress(bytesUploaded, bytesTotal) {
     *     const percentage = ((bytesUploaded / bytesTotal) * 100).toFixed(2);
     *     console.log('Uploaded ' + percentage + '%');
     *   },
     *   onSuccess() {
     *     console.log('Upload finished:', upload.url);
     *   },
     * });
     * 
     * <p>const previousUploads = await upload.findPreviousUploads();
     * 
     * <p>if (previousUploads.length &gt; 0) {
     *   upload.resumeFromPreviousUpload(previousUploads[0]);
     * }
     * 
     * <p>upload.start();
     * 
     * <p>```
     * 
     * <p>&gt; Note: If you are using tus from node.js, you need to add a custom URL
     * storage to enable resuming from previous uploads. On the browser, this
     * is enabled by default using local storage. In node.js, add urlStorage:
     * new tus.FileUrlStorage("path/to/tmp/file"), to the UploadFile object
     * definition above.
     * 
     * @return The call builder
     */
    public RequestUploadRequestBuilder create() {
        return new RequestUploadRequestBuilder(sdkConfiguration);
    }

    /**
     * Upload an asset
     * 
     * <p>To upload an asset, your first need to request for a direct upload URL
     * and only then actually upload the contents of the asset.
     * \
     * \
     * Once you created a upload link, you have 2 options, resumable or direct
     * upload. For a more reliable experience, you should use resumable uploads
     * which will work better for users with unreliable or slow network
     * connections. If you want a simpler implementation though, you should
     * just use a direct upload.
     * 
     * <p>## Direct Upload
     * For a direct upload, make a PUT request to the URL received in the url
     * field of the response above, with the raw video file as the request
     * body. response above:
     * 
     * <p>## Resumable Upload
     * Livepeer supports resumable uploads via Tus. This section provides a
     * simple example of how to use tus-js-client to upload a video file.
     * \
     * \
     * From the previous section, we generated a URL to upload a video file to
     * Livepeer on POST /api/asset/request-upload. You should use the
     * tusEndpoint field of the response to upload the video file and track the
     * progress:
     * 
     * <p>```
     * # This assumes there is an `input` element of `type="file"` with id
     * `fileInput` in the HTML
     * 
     * <p>const input = document.getElementById('fileInput');
     * 
     * <p>const file = input.files[0];
     * 
     * <p>const upload = new tus.Upload(file, {
     *   endpoint: tusEndpoint, // URL from `tusEndpoint` field in the
     * `/request-upload` response
     *   metadata: {
     *     filename,
     *     filetype: 'video/mp4',
     *   },
     *   uploadSize: file.size,
     *   onError(err) {
     *     console.error('Error uploading file:', err);
     *   },
     *   onProgress(bytesUploaded, bytesTotal) {
     *     const percentage = ((bytesUploaded / bytesTotal) * 100).toFixed(2);
     *     console.log('Uploaded ' + percentage + '%');
     *   },
     *   onSuccess() {
     *     console.log('Upload finished:', upload.url);
     *   },
     * });
     * 
     * <p>const previousUploads = await upload.findPreviousUploads();
     * 
     * <p>if (previousUploads.length &gt; 0) {
     *   upload.resumeFromPreviousUpload(previousUploads[0]);
     * }
     * 
     * <p>upload.start();
     * 
     * <p>```
     * 
     * <p>&gt; Note: If you are using tus from node.js, you need to add a custom URL
     * storage to enable resuming from previous uploads. On the browser, this
     * is enabled by default using local storage. In node.js, add urlStorage:
     * new tus.FileUrlStorage("path/to/tmp/file"), to the UploadFile object
     * definition above.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws Exception if the API call fails
     */
    public RequestUploadResponse create(NewAssetPayload request) throws Exception {
        RequestOperation<NewAssetPayload, RequestUploadResponse> operation
              = new RequestUpload.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Upload asset via URL
     * 
     * @return The call builder
     */
    public UploadAssetRequestBuilder createViaUrl() {
        return new UploadAssetRequestBuilder(sdkConfiguration);
    }

    /**
     * Upload asset via URL
     * 
     * @param request The request object containing all the parameters for the API call.
     * @return The response from the API call
     * @throws Exception if the API call fails
     */
    public UploadAssetResponse createViaUrl(NewAssetFromUrlPayload request) throws Exception {
        RequestOperation<NewAssetFromUrlPayload, UploadAssetResponse> operation
              = new UploadAsset.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Retrieves an asset
     * 
     * @return The call builder
     */
    public GetAssetRequestBuilder get() {
        return new GetAssetRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieves an asset
     * 
     * @param assetId ID of the asset
     * @return The response from the API call
     * @throws Exception if the API call fails
     */
    public GetAssetResponse get(String assetId) throws Exception {
        GetAssetRequest request =
            GetAssetRequest
                .builder()
                .assetId(assetId)
                .build();
        RequestOperation<GetAssetRequest, GetAssetResponse> operation
              = new GetAsset.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Patch an asset
     * 
     * @return The call builder
     */
    public UpdateAssetRequestBuilder update() {
        return new UpdateAssetRequestBuilder(sdkConfiguration);
    }

    /**
     * Patch an asset
     * 
     * @param assetId ID of the asset
     * @param assetPatchPayload 
     * @return The response from the API call
     * @throws Exception if the API call fails
     */
    public UpdateAssetResponse update(String assetId, AssetPatchPayload assetPatchPayload) throws Exception {
        UpdateAssetRequest request =
            UpdateAssetRequest
                .builder()
                .assetId(assetId)
                .assetPatchPayload(assetPatchPayload)
                .build();
        RequestOperation<UpdateAssetRequest, UpdateAssetResponse> operation
              = new UpdateAsset.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Delete an asset
     * 
     * @return The call builder
     */
    public DeleteAssetRequestBuilder delete() {
        return new DeleteAssetRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete an asset
     * 
     * @param assetId ID of the asset
     * @return The response from the API call
     * @throws Exception if the API call fails
     */
    public DeleteAssetResponse delete(String assetId) throws Exception {
        DeleteAssetRequest request =
            DeleteAssetRequest
                .builder()
                .assetId(assetId)
                .build();
        RequestOperation<DeleteAssetRequest, DeleteAssetResponse> operation
              = new DeleteAsset.Sync(sdkConfiguration, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
